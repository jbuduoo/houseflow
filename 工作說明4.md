
### A. `core\1_house.py` (資料抓取)  ==>>但看起來，選擇的仲介是選錯的，所以這部份，你幫我看是捉取的部份有問題，還是那邊有問題。
1.  **資料抓取 (Scraping)**：
    *   從網頁的 `.brandA` 區塊中尋找所有仲介來源。
    *   提取 `data-id`、圖片的 `alt` 屬性或文字內容作為初步的 `sourceName`。
    *   儲存網址 (URL) 以供後續辨識。

2.  **仲介選取 (`select_best_peer`)**：
    *   遍歷該物件的所有來源。
    *   若有 URL，優先透過 `1a_broker.py` 的網域映射表（`get_company_name`）轉換為正式名稱（如：`信義房屋`）。
    *   若無法透過網域辨識，則使用抓取到的 `sourceName`。
    *   **優先序比對**：使用 `BROKER_PRIORITY` 字典進行權重比對，數字越小越優先（越可靠）。


**推薦做法（同步更新兩個檔案）：**  ==>ok
```python
BROKER_PRIORITY = {
    '信義': 1, '永慶': 2, '好房網': 3, '住商': 4,
     '大家': 5, '591': 6,'21世紀': 7, 
    '太平洋': 8, '樂屋網': 9
}
