# 整合房仲爬蟲測試模組 (工作說明3) 實作計畫

根據您的「工作說明3」與架構偏好（保持模組化，多個方法互相呼叫，不一定要全塞在同一個檔案/資料夾），我整理了以下的整合實作計畫。

這份計畫旨在將 `房仲爬蟲測試/core/` 內的工具 (`utils.py`, `sinyi.py`, `geocoder.py`) 作為共用模組，讓現有的管線腳本進行呼叫與擴充。

## User Review Required
請確認以下幾點設計是否符合您的預期：

1. **模組擺放位置**: 我預計將 `utils.py`, `sinyi.py`, `geocoder.py` 從 `房仲爬蟲測試/core/` 搬移至主目錄的 `core/` 下 (其中 `utils.py` 建議改名為 `utils_broker.py` 避免撞名)。若您想把它們放在另一個專屬資料夾 (如 `core/spider/`)，請告訴我。
2. **`1_house.py` 的調整**: 目前 `1_house.py` 的 V 欄 (來源仲介) 是抓網頁上的標籤。根據您的說明，我會加入 `utils_broker.py` 的邏輯，如果遇到外部連結 (URL)，會用網域來自動辨識並覆寫/確保 V 欄的準確度。

## Open Questions
> [!WARNING]
> 關於 `[5] 補齊座標 (純補)` 中的 `4. 輸出: 分別寫入 R, S 欄 與 T, U 欄 =>刪除`：
> 請問您的意思是 **「這份說明文件中的這行字不需要存在 (文件排版修改)」**，還是 **「程式 `4_coords.py` 中不要把座標寫入試算表的 R, S, T, U 欄了」**？
> 如果程式不再將座標寫入試算表，這些新抓下來的座標該如何儲存呢？（在計畫中我先保留寫入邏輯，等待您的確認！）

## Proposed Changes

---

### 工具模組搬移 (共用工具庫)
為了讓各個主程式能方便呼叫這些方法，建議將新工具移至 `core/`。

#### [NEW] `core/utils_broker.py` (原 `utils.py`)
* 將原 `房仲爬蟲測試/core/utils.py` 移入，更名為 `utils_broker.py`。
#### [NEW] `core/sinyi.py`
* 將原 `房仲爬蟲測試/core/sinyi.py` 移入。
#### [NEW] `core/geocoder.py`
* 將原 `房仲爬蟲測試/core/geocoder.py` 移入。

---

### [1] 採集物件 (`core/1_house.py`)           ==>ok
#### [MODIFY] `core/1_house.py`
* 引入 `core/1_broker.py` 的網域萃取與房仲辨識方法。
* 在取得 `peerLinksData` 的外部連結時，呼叫 `utils_broker.get_company_name(extract_domain(url))` 來確保能精準寫入 V 欄 (來源仲介)。

---

### [5] 補齊座標 (純補) (`core/5_coords.py`)       ==>ok
#### [MODIFY] `core/5_coords.py`
* **整合 `sinyi.py`**: 引入 `core/sinyi.py` 的 `scrape_sinyi_coordinates` 方法。當 V 欄為「信義房屋」時，使用此新方法進行精準座標抓取，取代原有的初步爬蟲邏輯 (第一防線修改並加入)。
* **移除第三防線 (HF 備援)**: 刪除原本嘗試從 W, X 欄位讀取 HF 座標備援的邏輯 (`method_used == "HouseFlow備用"` 的部分刪除)。

---

### [6] 地址補完 (反查) - 新增模組            ==>ok
#### [NEW] `core/6_reverse_geocode.py`
* **功能**: 針對 V 欄為「信義房屋」，且已有座標 (R, S 欄)，但 M 欄 (查地址) 為空的資料進行反查。
* **實作**: 
    1. 引入 `core/geocoder.py`。
    2. 讀取 Google 試算表，篩選符合條件的列。
    3. 呼叫 `reverse_geocode(lat, lng)` 取得物理地址。
    4. 將結果格式化為 `[地址](座標反查)` (例如：`永和區永貞路３６２號五樓(座標反查)`)。
    5. 寫回試算表的 M 欄 (IDX_M_ADDR)。

#### [MODIFY] `1_master_run.py`             ==>ok
* 在選單中加入 `[6] 📍 地址補完 (座標反查) (6_reverse_geocode.py)`。
* 更新主執行迴圈，支援選項 6。

## Verification Plan
### 測試步驟
1. 確認 `core/1_house.py` 執行時能正常呼叫工具並寫入準確的 V 欄。
2. 執行 `1_master_run.py` 選項 `5` (`5_coords.py`)，驗證是否能成功調用 `sinyi.py` 抓取座標，且沒有觸發 HF 備援。
3. 執行新增的選項 `6` (`6_reverse_geocode.py`)，驗證 API 能將座標反查，並正確寫回 M 欄附帶 `(座標反查)` 標記。
