
# 2_deduplicate.py 
實施 P=Y 清除計畫：只要「是否已委託」(P 欄) 為 Y，就直接刪除（因為已經結案了）。
# 4_registry.py (戶籍/座標擷取)
取消戶籍OCR的讀取

# 在6之後，若沒有地址，也沒有定位，是不是就要刪除。



1. 實施「主從架構」合併（利用 2_deduplicate.py）
這是最有效的。與其讓試算表有 8000 筆資料，不如透過程式判斷「地址 + 樓層 + 坪數」，將同一個房子的多個刊登合併成一筆「主紀錄」：

主紀錄：保留最可靠仲介（如信義、永慶）的資訊。
從屬資訊 (AB 欄 JSON)：把剩下那 5、6 個刊登全部塞進 JSON。
成果：資料量可能會直接從 8000 筆縮減到 3000 筆左右，地圖會變得清爽非常多。
2. 建立「過期淘汰」機制
長期未更新：如果某個物件已經 3 個月沒在採集中出現了，它可能已經賣掉或下架了。
策略：在 2_deduplicate.py 中加入一個判斷，將太久沒更新的物件自動標記為「封存」或「隱藏」，不要讓它干擾新案子的開發。
3. 強化「AI 研判」與「標籤化」
價格異常：如果同一物件中，某個仲介報價突然降了 200 萬，這就是開發的大好機會。
標籤：讓 7_smart_analysis.py 自動標註「急售」、「價差大」、「多筆重複」等標籤。